# Lillith Offline Plugins

Drop folders here to add mini-apps to Lillith Offline.
Each plugin should include a `manifest.json` with:
- name
- description
- entry (UI route or component)
- capabilities
